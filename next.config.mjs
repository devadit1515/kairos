/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Render builds run `npm run build`; keep the output self-contained and fast.
  poweredByHeader: false,
};

export default nextConfig;
